///scr_dialog_activate_selected_option
 if(typewriter_active && typewriter_place - chars_drawn < 2)
                {
                    typewriter_place += 1000;
                }
                else
                    scr_dialog_action();
